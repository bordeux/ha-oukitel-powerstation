# Dev/CI tooling for the Oukitel HA integration
ruff>=0.6
pytest>=8
cryptography>=42        # used by protocol.py (also bundled in Home Assistant)
aiohttp>=3.9            # cloud.py HTTP client (also bundled in Home Assistant)
